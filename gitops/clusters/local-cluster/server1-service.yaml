apiVersion: v1
kind: Service
metadata:
  name: server1-service
  namespace: homelab-apps
spec:
  type: NodePort
  selector:
    app: server1
    version: blue
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30001
