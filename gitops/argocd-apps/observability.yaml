apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelab-observability
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/shaunsphere/devops-ai-platform-demo.git
    targetRevision: main
    path: platform/observability/kube-prometheus-stack

    helm:
      valueFiles:
        - values-enterprise.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
